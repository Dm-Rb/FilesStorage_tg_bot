Access files stored on a network drive via a Telegram bot.
The user stores photos of contracts on a network drive and has quick access to each contract via Telegram. The user sends a request to the bot, which searches for a partial or full match in the directory name and returns the images. 
